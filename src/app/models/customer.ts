export class Customer {

  public constructor(public id?: number, public first?: string,
                     public last?: string, public email?: string,
                     public password?: string) {

  }
}

// const mosh = new Customer();
// mosh.id = 123;
